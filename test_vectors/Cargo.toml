[package]
name = "test_vectors"
version = "0.0.0"
edition = "2018"

[dependencies]
blake3 = { path = "../" }
hex = "0.4.0"
reference_impl = { path = "../reference_impl" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
